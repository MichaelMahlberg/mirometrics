{
    "engines": {
        "node": "16.x"
    },
    "scripts": {
        "start": "",
        "test": "jest",
        "serve": "npx http-server -S -C localhost.crt -K localhost.key"
    },
    "devDependencies": {
        "http-server": "^13.0.0",
        "jest": "^26.0.1"
    }
}
